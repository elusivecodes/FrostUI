// Offcanvas default options
Offcanvas.defaults = {
    duration: 250,
    backdrop: true,
    keyboard: true,
    scroll: false
};

UI.initComponent('offcanvas', Offcanvas);

UI.Offcanvas = Offcanvas;
