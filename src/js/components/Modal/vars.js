// Modal default options
Modal.defaults = {
    duration: 250,
    backdrop: true,
    focus: true,
    show: false,
    keyboard: true
};

UI.initComponent('modal', Modal);

UI.Modal = Modal;
